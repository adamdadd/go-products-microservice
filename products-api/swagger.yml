basePath: /
consumes:
- application/json
definitions:
  Category:
    properties:
      description:
        type: string
        x-go-name: Description
      id:
        format: int64
        type: integer
        x-go-name: ID
      image_url:
        type: string
        x-go-name: ImageURL
      name:
        type: string
        x-go-name: Name
    type: object
    x-go-package: go-products-microservice/products-api/models
  Product:
    properties:
      description:
        type: string
        x-go-name: Description
      id:
        format: int64
        type: integer
        x-go-name: ID
      image_url:
        type: string
        x-go-name: ImageURL
      name:
        type: string
        x-go-name: Name
      price:
        format: float
        type: number
        x-go-name: PriceGBP
      sku:
        type: string
        x-go-name: SKU
    type: object
    x-go-package: go-products-microservice/products-api/models
info:
  description: Documentation for Product API.
  title: Products API.
  version: 0.0.1
paths:
  /categories/:
    get:
      description: Returns a list of products
      operationId: listCategories
      responses:
        "200":
          $ref: '#/responses/categoriesResponse'
      tags:
      - categories
    post:
      description: Adds a new product
      operationId: Category
      responses:
        "201":
          $ref: '#/responses/categoryResponse'
      tags:
      - categories
  /categories/{id}:
    delete:
      description: Deletes a product
      operationId: Category
      responses:
        "204":
          $ref: '#/responses/noBody'
      tags:
      - categories
    put:
      description: Updates an existing product
      operationId: Category
      responses:
        "201":
          $ref: '#/responses/categoryResponse'
      tags:
      - categories
  /products/:
    get:
      description: Returns a list of products
      operationId: listProducts
      responses:
        "200":
          $ref: '#/responses/productsResponse'
      tags:
      - products
    post:
      description: Adds a new product
      operationId: Product
      responses:
        "201":
          $ref: '#/responses/productResponse'
      tags:
      - products
  /products/{id}:
    delete:
      description: Deletes a product
      operationId: Product
      responses:
        "204":
          $ref: '#/responses/noBody'
      tags:
      - products
    put:
      description: Updates an existing product
      operationId: Product
      responses:
        "201":
          $ref: '#/responses/productResponse'
      tags:
      - products
produces:
- application/json
responses:
  categoriesResponse:
    description: ""
    schema:
      items:
        $ref: '#/definitions/Category'
      type: array
  categoryResponse:
    description: ""
    schema:
      $ref: '#/definitions/Category'
  noBody:
    description: ""
  productResponse:
    description: ""
    schema:
      $ref: '#/definitions/Product'
  productsResponse:
    description: ""
    schema:
      items:
        $ref: '#/definitions/Product'
      type: array
schemes:
- http
swagger: "2.0"
