basePath: /
consumes:
- application/json
definitions:
  Category:
    properties:
      description:
        type: string
        x-go-name: Description
      id:
        format: int64
        type: integer
        x-go-name: ID
      image_url:
        type: string
        x-go-name: ImageURL
      name:
        type: string
        x-go-name: Name
    type: object
    x-go-package: github.com/adamdadd/go-products-microservice/repository
  Product:
    properties:
      description:
        type: string
        x-go-name: Description
      id:
        format: int64
        type: integer
        x-go-name: ID
      image_url:
        type: string
        x-go-name: ImageURL
      name:
        type: string
        x-go-name: Name
      price:
        format: float
        type: number
        x-go-name: PriceGBP
      sku:
        type: string
        x-go-name: SKU
    type: object
    x-go-package: github.com/adamdadd/go-products-microservice/repository
info:
  description: Documentation for Product API.
  title: Products API.
  version: 0.0.1
paths:
  /categories/:
    get:
      description: Returns a list of products
      operationId: listCategories
      responses:
        "200":
          $ref: '#/responses/categoryResponse'
      tags:
      - categories
    post:
      description: Adds a new product
      operationId: Category
      responses:
        "201":
          $ref: '#/responses/201'
      tags:
      - categories
  /categories/{id}:
    delete:
      description: Deletes a product
      operationId: Category
      responses:
        "200":
          $ref: '#/responses/200'
      tags:
      - categories
    put:
      description: Updates an existing product
      operationId: Category
      responses:
        "201":
          $ref: '#/responses/201'
      tags:
      - categories
  /products/:
    get:
      description: Returns a list of products
      operationId: listProducts
      responses:
        "200":
          $ref: '#/responses/productResponse'
      tags:
      - products
    post:
      description: Adds a new product
      operationId: Product
      responses:
        "201":
          $ref: '#/responses/201'
      tags:
      - products
  /products/{id}:
    delete:
      description: Deletes a product
      operationId: Product
      responses:
        "200":
          $ref: '#/responses/200'
      tags:
      - products
    put:
      description: Updates an existing product
      operationId: Product
      responses:
        "201":
          $ref: '#/responses/201'
      tags:
      - products
produces:
- application/json
responses:
  categoryResponse:
    description: ""
    schema:
      items:
        $ref: '#/definitions/Category'
      type: array
  productResponse:
    description: ""
    schema:
      items:
        $ref: '#/definitions/Product'
      type: array
schemes:
- http
swagger: "2.0"
